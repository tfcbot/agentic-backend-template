


describe('Billing Integration Tests', () => {
  
  beforeEach(() => {
    
  });

  const headers = {
    'Content-Type': 'application/json',
    'Authorization': `Bearer ${process.env.AccessToken}`, 
  
  };

  test('User should be able to make a purchase', async () => {

  });


});
